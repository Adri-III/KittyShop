<p id="notice"><%= notice %></p>

<h1>Join Table Carts Items</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @join_table_carts_items.each do |join_table_carts_item| %>
      <tr>
        <td><%= link_to 'Show', join_table_carts_item %></td>
        <td><%= link_to 'Edit', edit_join_table_carts_item_path(join_table_carts_item) %></td>
        <td><%= link_to 'Destroy', join_table_carts_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Join Table Carts Item', new_join_table_carts_item_path %>
