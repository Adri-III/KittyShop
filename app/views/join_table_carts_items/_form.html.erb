<%= form_with(model: join_table_carts_item, local: true) do |form| %>
  <% if join_table_carts_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(join_table_carts_item.errors.count, "error") %> prohibited this join_table_carts_item from being saved:</h2>

      <ul>
      <% join_table_carts_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
